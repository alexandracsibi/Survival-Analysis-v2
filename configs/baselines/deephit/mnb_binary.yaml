seed: 42
device: auto

dataset:
  name: MNB
  data_root: data

model:
  name: DeepHit
  hidden_dims: [64, 32]
  dropout: 0.2

deephit:
  n_time_bins: 50
  binning: quantile
  alpha: 1.0
  beta: 0.0
  sigma: 0.1
  max_rank_pairs: 128

training:
  batch_size: 1024
  lr: 0.001
  weight_decay: 0.0001
  epochs: 1

early_stopping:
  enabled: false
  monitor: val_cindex
  mode: max
  patience: 5
  min_delta: 0.0001