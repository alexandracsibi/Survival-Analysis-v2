seed: 42
device: auto

dataset:
  name: synthetic_competing
  data_root: data

model:
  name: DeepHit
  hidden_dims: [64, 32]
  dropout: 0.2

deephit:
  n_time_bins: 50
  binning: quantile
  alpha: 1.0
  beta: 0.1
  sigma: 0.1
  max_rank_pairs: 128

training:
  batch_size: 512
  lr: 0.001
  weight_decay: 0.0001
  epochs: 75

early_stopping:
  enabled: true
  monitor: val_mean_cindex
  mode: max
  patience: 5
  min_delta: 0.0001